{
  "name": "numbrrr",
  "version": "1.0.0",
  "description": "NumBrrr — calculate the total savings you need to become financially free",
  "main": "app.js",
  "scripts": {
    "test": "node --check app.js && node --check api/quote.js && node --check api/ipo.js && node --check api/topperf.js"
  },
  "keywords": [
    "finance",
    "calculator",
    "fire",
    "financial-independence"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
